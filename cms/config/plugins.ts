export default () => {
  const config: Record<string, unknown> = {};

  // Azure Blob Storage provider can be added here for production
  // when a compatible Strapi 5 provider package is available.

  return config;
};
